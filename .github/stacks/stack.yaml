name: React App
description: This repository test stack version migration

branding:
  icon: 'package'
  color: 'yellow'

configs:
  repo-metadata:
    parameters:
      description: "A react-app template"
      topics:
        - react-app
        - nodejs
        - vercel
        

init:
  uses: stack-init.yaml
